/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Add other Next.js configurations here if needed in the future
  // For example, to use the src directory:
  // pageExtensions: ['tsx', 'ts', 'jsx', 'js'], // Already default if src/pages exists
  // compiler: {
  //   styledComponents: true, // if you were using styled-components
  // },
};

export default nextConfig;
